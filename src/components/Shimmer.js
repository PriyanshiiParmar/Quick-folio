import { useEffect, useState } from "react";
import { useSelector } from "react-redux";

const Shimmer = () => {
  return (
    <div>
      <div>Data has been saved to local storage successfully!</div>
    </div>
  );
};
export default Shimmer;
